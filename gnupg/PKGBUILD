# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=gnupg
_realver=20210407
_choconame=gnupg
pkgbase="app-${_realname}"
pkgname=("app-i686-${_realname}")
epoch=1
pkgver=2.3.0
pkgrel=1
pkgdesc="Complete and free implementation of the OpenPGP standard"
url="https://www.gnupg.org/"
arch=('any')
license=('custom')
makedepends=("p7zip")
provides=("${pkgbase}")
options=(!strip)
source=("gnupg-w32.exe::https://www.gnupg.org/ftp/gcrypt/binary/gnupg-w32-${pkgver}_${_realver}.exe")
sha512sums=('fd6d689664e656bcb2330943c09c3a488749c4f383b27d3cc23058e66b1edcc20f7b01e5ca5d5848d8394dcdd63a51067a5486946130081c72142fe159434d48')

prepare() {
    7z x -y -bd "gnupg-w32.exe" -o"gnupg-w32.dir" > /dev/null
}

package() {
    local a="${pkgdir}/apps/${_realname}"
    mkdir -p "${a}"
    cp -R "gnupg-w32.dir"/* "${a}"
    rm -R "${a}"/'$PLUGINSDIR' "${a}/gnupg-uninstall.exe.nsis"
    find "${a}" -iname '*.tmp' -delete
}
