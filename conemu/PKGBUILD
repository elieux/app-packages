# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=conemu
pkgbase="app-${_realname}"
pkgname=("app-i686-${_realname}"
         "app-x86_64-${_realname}")
pkgver=16.06.19
pkgrel=1
pkgdesc="A powerful, highly customizable console emulator with tabs"
url="http://conemu.github.io/"
arch=('any')
license=('BSD')
makedepends=("p7zip")
provides=("${pkgbase}")
options=(!strip)
source=("ConEmuPack.7z::https://github.com/Maximus5/ConEmu/releases/download/v${pkgver}/ConEmuPack.${pkgver//./}.7z")
sha512sums=('e1efb87a8c52221e41e61c3a381f834cd231a46ca60eb6bd6938c4828db67a9a69f936fd0f56ce5b1bad9186da0a70c990bcf5b094f1eee929a3a3346a8fe802')

_lc() {
    for f in "${@}"
    do
        n="$(echo "${f}" | tr 'A-Z' 'a-z')"
        if test "${f}" != "${n}"
        then
            mv "${f}" "${n}"
        fi
    done
}

prepare() {
    _lc *
}

_package() {
    a="${pkgdir}/apps${_apparch}/${_realname}"
    mkdir -p "${a}"
    cp -r "conemu${1}.exe" "${a}/conemu.exe"
    cp -r "conemu" "plugins" "${a}"

    d="${pkgdir}/docs${_apparch}/${_realname}"
    mkdir -p "${d}"
    mv "${a}/conemu"/*.txt "${d}"
}

package_app-i686-conemu() {
    _apparch=32
    _package ""
}

package_app-x86_64-conemu() {
    _apparch=64
    _package "64"
}
