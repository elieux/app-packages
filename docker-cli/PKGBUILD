# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=docker-cli
_choconame=docker-cli
_dirname=docker
pkgbase="app-${_realname}"
pkgname=("app-x86_64-${_realname}")
epoch=1
pkgver=26.1.3
pkgrel=1
pkgdesc="Pack, ship and run any application as a lightweight container"
url="https://docs.docker.com/engine/reference/commandline/cli/"
arch=('any')
license=('Apache')
provides=("${pkgbase}")
replaces=("app-x86_64-docker")
conflicts=("app-x86_64-docker")
options=(!strip)
source=("docker.zip::https://download.docker.com/win/static/stable/x86_64/docker-${pkgver}.zip")
sha512sums=('6e8cc3e67bb2da915795885505368c7c584c47938810c3e86d9dae653af455c3fccdea0f37e0f5b065dca62d1d21c9a89cca720542647467b8af27adf3e6c59e')

package() {
    local a="${pkgdir}/apps/${_dirname}"
    mkdir -p "${a}"
    cp "docker/docker.exe" "${a}/"
}
