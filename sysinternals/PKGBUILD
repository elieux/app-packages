# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=sysinternals
pkgname=("app-i686-${_realname}")
pkgver=2014.09.11
pkgrel=1
pkgdesc="Advanced system utilities for Windows"
url="http://technet.microsoft.com/en-us/sysinternals/bb545021.aspx"
arch=('any')
license=('custom')
makedepends=("unzip")
options=(!strip) # digital signatures
source=("http://download.sysinternals.com/files/SysinternalsSuite.zip"
        "http://download.sysinternals.com/files/BlueScreen.zip")
md5sums=('5aab88840d3f3cbb3d631bd317bf423d'
         'e985acdd52b7bccdd6feb85ebd74c722')

prepare() {
    cd "${srcdir}"

    for f in *
    do
        n=$(echo "${f}" | tr 'A-Z' 'a-z')
        if test "${f}" != "${n}"
        then
            mv "${f}" "${n}"
        fi
    done
}

package() {
    apparch=32
    mkdir -p "${pkgdir}/apps${apparch}/${_realname}"
    mkdir -p "${pkgdir}/docs${apparch}/${_realname}"
    cp "${srcdir}"/*.{exe,sys,scr} "${pkgdir}/apps${apparch}/${_realname}/"
    cp "${srcdir}"/*.{chm,txt,hlp} "${pkgdir}/docs${apparch}/${_realname}/"
}
