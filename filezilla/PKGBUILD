# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=filezilla
_choconame=filezilla
pkgbase="app-${_realname}"
pkgname=("app-x86_64-${_realname}")
epoch=1
pkgver=3.63.1
pkgrel=1
pkgdesc="Fast and reliable FTP, FTPS and SFTP client"
url="https://filezilla-project.org/"
arch=('any')
license=('GPL')
replaces=("app-i686-${_realname}")
provides=("${pkgbase}")
options=(!strip)
source=("FileZilla_win64.zip::https://download.filezilla-project.org/client/FileZilla_${pkgver}_win64.zip")
sha512sums=('67add3c597d2d65c3616409322fcfa0b156a66432d9a51fbe9334e775d1fca68fe2c841f90e77e0427e3bdbd1d4b069e1b5aec91e8ecd79d4017ee1c2cf31fe4')

package() {
    local a="${pkgdir}/apps/${_realname}"
    mkdir -p "${a}"
    cp -R "FileZilla-${pkgver}"/* "${a}"
}
