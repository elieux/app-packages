# Maintainer: David Macek <david.macek.0@gmail.com>

_extract_subdir() {
    local _file="$(get_filename "${1}")"
    local _dir="${_file%.*}"
    if type bsdtar | head -1 | grep -q 'is a function'
    then
        local _original_bsdtar="$(type bsdtar | tail +2)"
    fi
    bsdtar() {
        local _extra_args=()
        if [ "${1}" = "-xf" ]
        then
            mkdir -p "${_dir}"
            _extra_args=(-C "${_dir}")
        fi
        /usr/bin/bsdtar "${_extra_args[@]}" "${@}"
    }
    extract_file "${_file}"
    unset -f bsdtar
    eval "${_original_bsdtar}"
}

extract_https@subdir() {
    _extract_subdir "${@}"
}

download_https@subdir() {
    download_file "${1/@subdir:/:}"
}

_realname=innounp
pkgbase="app-${_realname}"
pkgname=("app-i686-${_realname}")
epoch=1
pkgver=2.67.3.0
pkgrel=1
pkgdesc="Inno Setup unpacker"
url="https://www.rathlev-home.de/index-e.html?tools/prog-e.html#unpack"
arch=('any')
license=('MIT' 'GPL')
provides=("${pkgbase}")
options=(!strip)
source=("innounpacker.zip::https@subdir://www.rathlev-home.de/tools/download/innounpacker.zip")
sha512sums=('10d5ee9c666e8a76981dfcc80c72614f68023c3ccabe2f7100685788d1209c0df7448b8bf8071f8e9203e2c0bd6c66ed7b457573de624209f6cd6b16100737ad')

pkgver() {
    powershell -command '(Get-Item "innounpacker/innounp.exe").VersionInfo.ProductVersionRaw.ToString()'
}

package() {
    local a="${pkgdir}/apps/${_realname}"
    mkdir -p "${a}"
    cp "innounpacker"/* "${a}"
}
