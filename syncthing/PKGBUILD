# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=syncthing
pkgbase="app-${_realname}"
pkgname=("app-x86_64-${_realname}")
epoch=1
pkgver=1.7.1
pkgrel=1
pkgdesc="Continuous file synchronization"
url="https://syncthing.net/"
arch=('any')
license=('MPL')
provides=("${pkgbase}")
options=(!strip)
source=("syncthing-windows-amd64.zip::https://github.com/syncthing/syncthing/releases/download/v${pkgver}/syncthing-windows-amd64-v${pkgver}.zip")
sha512sums=('8689cea7477d98a1588c64cf4c9bd1101ecccc92b062ab003e8696d1507fe8622e0dbbfdb99503369b5a598e477eb71f40d1ca706780900092016bcd389d6627')

package() {
    local a="${pkgdir}/apps/${_realname}"
    mkdir -p "${a}"
    cp -R "syncthing-windows-amd64-v${pkgver}"/* "${a}"
}
