# Maintainer: David Macek <david.macek.0@gmail.com>

_realname=perfmonitor
_dirname=cpuid
pkgbase="app-${_realname}"
pkgname=("app-x86_64-${_realname}")
epoch=1
pkgver=2.04
pkgrel=1
pkgdesc="Monitors processor performance and other values (CPUID)"
url="http://www.cpuid.com/softwares/perfmonitor-2.html"
arch=('any')
license=('custom')
replaces=("app-i686-${_realname}")
makedepends=("unzip")
provides=("${pkgbase}")
options=(!strip)
source=("perfmonitor-2.zip::http://download.cpuid.com/perfmonitor-2/perfmonitor-2_${pkgver}.zip")
sha512sums=('0688224b3ccf87ff3ef90ea5bc8dc615b581fec29ec9a2c8683ebf45f44a58207b29aa663d8f36edecb768d6a7f617265ad59de4466974fa89870f25bdee9630')

_lc() {
    local d f n

    for f in "${@}"
    do
        d="$(dirname "${f}")"
        f="$(basename "${f}")"
        n="$(echo "${f}" | tr 'A-Z' 'a-z')"
        if test "${f}" != "${n}"
        then
            mv "${d}/${f}" "${d}/${n}"
        fi
    done
}

prepare() {
    _lc *
}

package() {
    a="${pkgdir}/apps/${_dirname}"
    mkdir -p "${a}"
    cp "perfmonitor2_x64.exe" "${a}/perfmonitor2.exe"
    cp "perfmonitor2.pdf" "${a}"
}
